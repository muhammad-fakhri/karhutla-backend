/**
 * Get digit length
 * @param {number} number input digit
 * @returns {boolean} true if string in a valid phone number and vice versa
 */
export const digitLength = (number: number): number => number.toString().length
